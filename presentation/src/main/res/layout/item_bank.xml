<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardBank"
    style="@style/BankCardViewStyle"
    android:layout_width="match_parent"
    android:layout_height="@dimen/list_bank_element_height"
    card_view:cardBackgroundColor="@color/defaultColorItemBank"
    android:foreground="?android:attr/selectableItemBackground"
    android:clickable="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="@dimen/padding_item_bank_top"
        android:paddingBottom="@dimen/padding_item_bank_bottom"
        android:paddingStart="@dimen/padding_item_bank_start"
        android:paddingEnd="@dimen/padding_item_bank_end">

        <ImageView
            android:id="@+id/image_bank"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerInParent="true"
            android:scaleType="fitCenter"
            android:src="@drawable/logo_tinkoff" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>